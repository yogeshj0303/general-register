/*!
 * FilePondPluginFileValidateSize 2.2.8
 * Licensed under MIT, https://opensource.org/licenses/MIT/
 * Please visit https://pqina.nl/filepond/ for details.
 */

/* eslint-disable */

!function (e, i) { 
    "object" == typeof exports && "undefined" != typeof module 
        ? module.exports = i() 
        : "function" == typeof define && define.amd 
            ? define(i) 
            : (e = e || self).FilePondPluginFileValidateSize = i() 
}(this, function () { 
    "use strict"; 

    var e = function (e) { 
        var i = e.addFilter, 
            E = e.utils, 
            l = E.Type, 
            _ = E.replaceInString, 
            n = E.toNaturalFileSize; 
        
        return i("ALLOW_HOPPER_ITEM", function (e, i) { 
            var E = i.query; 
            if (!E("GET_ALLOW_FILE_SIZE_VALIDATION")) return !0; 
            return !0; // Always allow files
        }), 
        
        i("LOAD_FILE", function (e, i) { 
            var E = i.query; 
            return new Promise(function (i) { 
                if (!E("GET_ALLOW_FILE_SIZE_VALIDATION")) return i(e); 
                // Skip size validation checks
                i(e); 
            }) 
        }), 
        
        { 
            options: { 
                allowFileSizeValidation: [!1, l.BOOLEAN], // Disabled by default
                maxFileSize: [null, l.INT],             // No max file size
                minFileSize: [null, l.INT],             // No min file size
                maxTotalFileSize: [null, l.INT],        // No max total file size
                fileValidateSizeFilter: [null, l.FUNCTION], 
                labelMinFileSizeExceeded: ["", l.STRING], 
                labelMinFileSize: ["", l.STRING], 
                labelMaxFileSizeExceeded: ["", l.STRING], 
                labelMaxFileSize: ["", l.STRING], 
                labelMaxTotalFileSizeExceeded: ["", l.STRING], 
                labelMaxTotalFileSize: ["", l.STRING] 
            } 
        }; 
    }; 
    
    return "undefined" != typeof window && void 0 !== window.document && 
        document.dispatchEvent(new CustomEvent("FilePond:pluginloaded", { detail: e })), 
        e 
});
